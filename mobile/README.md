# Bike Tracker - Mobile (Expo)

## Setup
1. Install dependencies:
   ```bash
   npm install
   ```
2. Edit `App.js` and set `API_URL` to your backend URL (e.g. http://192.168.0.100:4000).
3. Run:
   ```bash
   npm start
   ```
4. Open Expo Go on your phone, or run on emulator.
